import { StyledViewProps } from "@components/view/interface";
import styled from "@emotion/native";
import FastImage from "react-native-fast-image";

export const BaseImage = styled(FastImage)<StyledViewProps>(({
}) => ({
}));