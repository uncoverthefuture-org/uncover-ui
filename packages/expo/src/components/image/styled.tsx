import styled from "@emotion/native";
import { StyledViewProps } from "@uncover-ui/react-native/lib/components/view/interface";
import { Image } from "expo-image";

export const BaseImage = styled(Image)<StyledViewProps>(({
}) => ({
}));